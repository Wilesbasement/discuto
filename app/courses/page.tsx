import Link from "next/link";
import { SiteNav } from "@/components/site-nav";
import { getCourses } from "@/lib/courses";
export default async function CoursesPage({searchParams}:{searchParams?:{q?:string}}){const q=searchParams?.q||""; const courses=await getCourses(q); return <main><SiteNav/><section className="page-shell"><div className="container page-stack"><div><span className="eyebrow">Course directory</span><h1 className="dashboard-title">Disc Golf Courses</h1><p className="dashboard-copy">Search real course data and open a course to view activity, check-ins, and best scores.</p></div><form className="panel" action="/courses"><label>Search courses<input className="form-input" name="q" defaultValue={q} placeholder="Course, city, state, zip"/></label><button className="button" style={{marginTop:14}}>Search</button></form><div className="dashboard-feed">{courses.map(c=><Link className="post-card" href={`/courses/${c.id}`} key={c.id}><div className="post-card-top"><div><p className="post-author">{c.name}</p><p className="post-meta">{c.city||"Unknown city"} • {c.state||"Unknown state"} {c.zip?`• ${c.zip}`:""}</p></div><span className="badge">{c.holeCount?`${c.holeCount} holes`:"View"}</span></div><p className="post-content">{c.rating?`Rating: ${c.rating}`:"Open course page"}</p></Link>)}</div></div></section></main>}
