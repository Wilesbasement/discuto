import Link from "next/link";
import { SiteNav } from "@/components/site-nav";
import { getGlobalLeaderboard } from "@/lib/community";
export default async function Leaderboard({searchParams}:{searchParams?:{course?:string}}){const course=searchParams?.course; const rounds=await getGlobalLeaderboard(course); return <main><SiteNav/><section className="page-shell"><div className="container page-stack"><div><span className="eyebrow">Leaderboard</span><h1 className="dashboard-title">{course?"Course leaderboard":"Global leaderboard"}</h1><p className="dashboard-copy">Best posted scores from DiscPlus players.</p></div><section className="panel">{rounds.length?<div className="dashboard-feed">{rounds.map((r:any,i:number)=>{const p=r.profiles; const name=p?.display_name||p?.username||"DiscPlus player"; return <article className="post-card" key={r.id}><div className="post-card-top"><div><p className="post-author">#{i+1} • {r.total_score} • {name}</p><p className="post-meta">{r.course_name||r.course_id} • {r.holes_played||18} holes</p></div><Link className="badge" href={`/courses/${r.course_id}`}>Course</Link></div></article>})}</div>:<p className="dashboard-copy">No scores yet. Log in, open a course, and post the first score.</p>}</section></div></section></main>}
